[package]
name = "aios-proto"
version = "0.0.1"
edition = "2021"

[lib]
name = "aios_proto"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
